.pull-right= link_to 'Send', send_pdf_user_path(@user), method: :post, class: 'btn btn-info'
%h1 User's profile
- if @user.avatar.present?
  %p= image_tag @user.avatar.thumb.url
%p= @user.full_name
%p= @user.email
%p= @user.birth_date


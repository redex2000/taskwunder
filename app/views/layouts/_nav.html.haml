- if user_signed_in?
  %nav.navbar
    %ul.nav.navbar-nav
      - if policy(User).index?
        %li= link_to 'All users', users_path
      %li{ class: 'active' }= link_to 'Sign out', destroy_user_session_path, method: :delete